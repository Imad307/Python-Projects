I used pandas and a csv file to make a dataframe then iterate that dataframe rowise using iterrows() and adding letters and alphabets to a dictionary of NATO phonetic alphabet. 
Then I made a function where I took user input and iterate it through while comparing the characters with dictionary of NATO phonetic alphabet in a list comprehension which is then printed out. 
I also created a while True loop which keep running until user chooses to deny further inputs. 
